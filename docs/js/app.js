define(['router'], function(Router) {
    return {
        initialize: function() {
            // tpl.loadTemplates(['drawer-menu-item'], function() {
            var router = new Router();
            // });
        },
    };
});