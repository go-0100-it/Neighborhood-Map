define([
        'jquery',
        'backbone',
        'underscore',
        'knockout',
        'router'
    ],
    function($, Backbone, _, ko) {
        var WeatherListViewModel = function(place) {
            var _this = this;

            return this;
        };
        return WeatherListViewModel;
    });